# ⚠️ КРИТИЧЕСКИ ВАЖНО: Перезапустите Vite!

## Проблема
Статические файлы возвращают 404, потому что Vite все еще использует старую конфигурацию.

## Решение

### 1. Остановите текущий Vite dev server

Найдите терминал, где запущен Vite для app, и нажмите **Ctrl+C**.

### 2. Запустите Vite заново

```powershell
cd app
npm run dev
```

### 3. Проверьте, что Vite работает на правильном пути

После перезапуска проверьте:

```powershell
# Должен вернуть HTML
curl http://localhost:3000/app/

# Должен вернуть Vite client
curl http://localhost:3000/app/@vite/client

# Должен вернуть JS файл
curl http://localhost:3000/app/src/main.jsx
```

Все должны вернуть успешный ответ (не 404).

### 4. Проверьте через прокси

```powershell
curl http://localhost:8082/app/
curl http://localhost:8082/app/@vite/client
```

### 5. Проверьте через туннель

```powershell
curl https://turns-bloomberg-greatest-caused.trycloudflare.com/app/
```

---

## ⚠️ Почему это важно?

Vite читает `vite.config.js` только при запуске. Если вы изменили конфигурацию, но не перезапустили Vite, он все еще использует старую конфигурацию.

## 🔍 Как проверить, что Vite перезапущен?

1. Проверьте время запуска процесса Vite
2. Проверьте, что `http://localhost:3000/app/` возвращает HTML
3. Проверьте логи Vite - там должно быть сообщение о запуске

## 📋 Текущая конфигурация

- **Vite base:** `/app/` (всегда)
- **Vite порт:** 3000
- **Прокси порт:** 8082
- **Cloudflare Tunnel:** направлен на 8082

После перезапуска Vite все должно работать!
