# 🔧 Исправление ошибок 404 для статических файлов

## Проблема
Статические файлы (JS, CSS) не загружаются, возвращается 404.

## Причина
После изменения `vite.config.js` нужно перезапустить Vite dev server, чтобы изменения вступили в силу.

## Решение

### 1. Перезапустите Vite dev server для app

**Обязательно!** Изменения в `vite.config.js` вступят в силу только после перезапуска:

```powershell
# Остановите текущий Vite (Ctrl+C в терминале, где он запущен)
# Затем запустите заново:
cd app
npm run dev
```

### 2. Проверьте, что Vite работает на корневом пути

После перезапуска проверьте:

```powershell
# Должен вернуть HTML
curl http://localhost:3000/

# Должен вернуть JS файл
curl http://localhost:3000/src/main.jsx

# Должен вернуть Vite client
curl http://localhost:3000/@vite/client
```

### 3. Проверьте работу через прокси

```powershell
# Должен вернуть HTML
curl http://localhost:8082/app/

# Должен вернуть JS файл (прокси уберет /app)
curl http://localhost:8082/app/src/main.jsx
```

### 4. Проверьте через туннель

```powershell
curl https://turns-bloomberg-greatest-caused.trycloudflare.com/app/
```

---

## ⚠️ Важно

- **Обязательно перезапустите Vite** после изменения `vite.config.js`
- Vite должен работать на порту **3000**
- Прокси должен работать на порту **8082**
- Cloudflare Tunnel должен быть направлен на порт **8082**

## 🔍 Если проблема сохраняется

1. Проверьте логи Vite - там должны быть запросы
2. Проверьте логи прокси - там должны быть запросы к статическим файлам
3. Убедитесь, что Vite действительно перезапущен (проверьте время запуска процесса)

## 📋 Текущая конфигурация

- **Vite base (dev):** `/` (корневой путь)
- **Vite base (production):** `/app/`
- **React Router basename:** `/app`
- **Прокси pathRewrite:** убирает `/app` при проксировании
