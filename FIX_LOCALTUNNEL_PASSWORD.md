# 🔐 Решение проблемы с паролем localtunnel

## Проблема
Localtunnel запрашивает пароль при доступе к туннелю.

## Решение 1: Установить пароль при запуске

При запуске localtunnel можно установить пароль:

```powershell
lt --port 8080 --subdomain pretty-times-try
```

Или с паролем:
```powershell
lt --port 8080 --subdomain pretty-times-try --local-host localhost
```

Но лучше использовать опцию без пароля или другой туннель.

## Решение 2: Использовать Cloudflare Tunnel (рекомендуется)

Cloudflare Tunnel не требует пароля и более стабилен.

### Установка Cloudflare Tunnel

**Windows (без админ прав):**
1. Скачайте: https://github.com/cloudflare/cloudflared/releases/latest
2. Распакуйте `cloudflared-windows-amd64.exe`
3. Переименуйте в `cloudflared.exe`
4. Поместите в папку (например, `C:\Users\user\cloudflared\`)

### Использование

```powershell
# Перейдите в папку с cloudflared
cd C:\Users\user\cloudflared

# Запустите туннель
.\cloudflared.exe tunnel --url http://localhost:8080
```

Вы получите URL типа: `https://random-name.trycloudflare.com`

**Преимущества:**
- ✅ Не требует пароля
- ✅ Более стабильный
- ✅ Быстрый (от Cloudflare)
- ✅ Бесплатный

## Решение 3: Использовать localhost.run (через SSH)

Если у вас установлен SSH клиент:

```powershell
ssh -R 80:localhost:8080 ssh.localhost.run
```

**Преимущества:**
- ✅ Не требует пароля
- ✅ Не требует установки (если SSH уже есть)
- ✅ Бесплатный

## Решение 4: Использовать Pinggy.io

1. Откройте https://pinggy.io
2. Скопируйте команду для Windows
3. Запустите в терминале

**Преимущества:**
- ✅ Веб-интерфейс
- ✅ Логи запросов
- ✅ Не требует пароля

---

## 🎯 Рекомендация

**Используйте Cloudflare Tunnel** - он самый надежный и не требует пароля.

После получения нового URL:
1. Обновите `backend/.env` с новым URL
2. Запустите `npm run setup-mini-apps` в папке `backend`
