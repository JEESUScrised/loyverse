# Быстрый деплой для демо

## Вариант 1: Vercel (Рекомендуется - самый быстрый) ⚡

### Шаг 1: Подготовка
1. Убедитесь, что проект загружен в GitHub/GitLab/Bitbucket
2. Зарегистрируйтесь на [vercel.com](https://vercel.com) (можно через GitHub)

### Шаг 2: Деплой каждого приложения

#### Деплой основного приложения (app):
1. Зайдите на [vercel.com/new](https://vercel.com/new)
2. Импортируйте ваш репозиторий
3. В настройках проекта:
   - **Root Directory**: `app`
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
4. Нажмите Deploy
5. Получите URL вида: `https://your-app-name.vercel.app`

#### Деплой приложения кассира (cashier):
1. Создайте новый проект в Vercel
2. Выберите тот же репозиторий
3. В настройках:
   - **Root Directory**: `cashier`
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
4. Deploy

#### Деплой приложения владельца (owner):
1. Создайте новый проект в Vercel
2. Выберите тот же репозиторий
3. В настройках:
   - **Root Directory**: `owner`
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
4. Deploy

### Преимущества Vercel:
- ✅ Бесплатный для демо
- ✅ Автоматический HTTPS
- ✅ Деплой за 1-2 минуты
- ✅ Автоматический деплой при push в GitHub
- ✅ Можно добавить кастомный домен

---

## Вариант 2: Netlify (Альтернатива)

### Быстрый деплой через Netlify Drop:
1. Зайдите на [app.netlify.com/drop](https://app.netlify.com/drop)
2. Соберите каждое приложение локально:
   ```bash
   cd app
   npm install
   npm run build
   ```
3. Перетащите папку `app/dist` в Netlify Drop
4. Получите URL сразу

**Недостаток**: Нужно передеплоивать вручную при изменениях

### Деплой через Git (лучше):
1. Зарегистрируйтесь на [netlify.com](https://netlify.com)
2. New site from Git → выберите репозиторий
3. Настройки:
   - **Base directory**: `app`
   - **Build command**: `npm run build`
   - **Publish directory**: `app/dist`
4. Deploy site

Повторите для `cashier` и `owner` как отдельные сайты.

---

## Вариант 3: GitHub Pages (Бесплатно, но сложнее)

1. Установите `gh-pages`:
   ```bash
   npm install -D gh-pages --workspace-root
   ```

2. Добавьте скрипты в package.json каждого приложения:
   ```json
   "scripts": {
     "deploy": "npm run build && gh-pages -d dist"
   }
   ```

3. Деплой:
   ```bash
   cd app && npm run deploy
   ```

**Недостаток**: Нужно настраивать base path в vite.config.js

---

## Рекомендация для быстрого демо

**Используйте Vercel** - это самый быстрый способ:
- 3-5 минут на настройку
- Автоматический деплой
- Готовые HTTPS URL для каждого приложения
- Можно показать коллеге сразу после деплоя

### После деплоя:
- Основное приложение: `https://loyverse-app.vercel.app`
- Кассир: `https://loyverse-cashier.vercel.app`
- Владелец: `https://loyverse-owner.vercel.app`

Отправьте эти ссылки коллеге! 🚀
