# 🔧 Исправление загрузки статических файлов Vite

## Проблема
Статические файлы Vite (`@vite/client`, `@react-refresh`, `/src/main.jsx`) возвращают 404.

## Причина
Vite генерирует пути к статическим файлам на основе `base` в `vite.config.js`. Когда приложение открывается через `/app/`, все пути должны иметь префикс `/app/`.

## Решение

### 1. Обновлен `app/vite.config.js`

Теперь всегда используется `base: '/app/'`:
- В dev режиме: `/app/`
- В production: `/app/`

Это гарантирует, что все статические файлы будут иметь правильные пути.

### 2. Обновлен `proxy-server-express.js`

Прокси теперь передает пути как есть (без переписывания), так как Vite настроен на `/app/`.

### 3. Перезапустите Vite dev server

**Обязательно!** Изменения в `vite.config.js` вступят в силу только после перезапуска:

```powershell
# Остановите текущий Vite (Ctrl+C в терминале, где он запущен)
# Затем запустите заново:
cd app
npm run dev
```

### 4. Проверьте работу

После перезапуска Vite проверьте:

```powershell
# Должен вернуть Vite client
curl http://localhost:3000/app/@vite/client

# Должен вернуть JS файл
curl http://localhost:3000/app/src/main.jsx

# Через прокси
curl http://localhost:8082/app/@vite/client
curl http://localhost:8082/app/src/main.jsx

# Через туннель
curl https://turns-bloomberg-greatest-caused.trycloudflare.com/app/@vite/client
```

Все должны вернуть успешный ответ (не 404).

---

## ⚠️ Важно

- **Обязательно перезапустите Vite** после изменения `vite.config.js`
- Vite должен работать на порту **3000**
- Прокси должен работать на порту **8082**
- Cloudflare Tunnel должен быть направлен на порт **8082**

## 🔍 Если проблема сохраняется

1. Проверьте, что Vite перезапущен
2. Проверьте, что Vite работает на `http://localhost:3000/app/`
3. Проверьте логи прокси - там должны быть запросы к статическим файлам
4. Убедитесь, что в браузере нет кеша (Ctrl+Shift+R для жесткой перезагрузки)

## 📋 Текущая конфигурация

- **Vite base:** `/app/` (всегда)
- **React Router basename:** `/app`
- **Прокси:** передает пути как есть (без переписывания)
