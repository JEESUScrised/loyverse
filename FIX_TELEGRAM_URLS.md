# 🔧 Исправление URL в Telegram Mini Apps

## Проблема
Telegram пытается открыть приложение по старому URL от ngrok, который больше не работает.

## Решение

### 1. Обновите `backend/.env`

Откройте `backend/.env` и замените URL на новые от localtunnel:

```env
CLIENT_APP_URL=https://pretty-times-try.loca.lt/app
OWNER_APP_URL=https://pretty-times-try.loca.lt/owner
CASHIER_APP_URL=https://pretty-times-try.loca.lt/cashier
```

### 2. Перезапустите бэкенд (если запущен)

```powershell
# Остановите бэкенд (Ctrl+C)
# Затем запустите заново:
cd backend
npm start
```

### 3. Настройте Mini Apps в Telegram

```powershell
cd backend
npm run setup-mini-apps
```

Это обновит URL в Telegram для всех трех ботов (клиент, владелец, кассир).

### 4. Проверьте результат

После выполнения `setup-mini-apps` вы должны увидеть:
```
✅ Клиентский бот: Mini App настроен успешно
   URL: https://pretty-times-try.loca.lt/app
✅ Бот владельца: Mini App настроен успешно
   URL: https://pretty-times-try.loca.lt/owner
✅ Бот кассира: Mini App настроен успешно
   URL: https://pretty-times-try.loca.lt/cashier
```

### 5. Попробуйте открыть приложение в Telegram снова

Теперь должно работать!

---

## ⚠️ Важно

- URL от localtunnel меняется при каждом перезапуске
- Если перезапустите `lt --port 8080`, получите новый URL
- Нужно будет снова обновить `.env` и запустить `setup-mini-apps`

## 💡 Для стабильного URL

Если нужен постоянный URL, используйте Cloudflare Tunnel (см. `NGROK_ALTERNATIVES.md`).
