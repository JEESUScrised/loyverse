# 🔄 Альтернативы ngrok

## 🥇 Рекомендация: Localtunnel (самый простой)

### Установка
```powershell
npm install -g localtunnel
```

### Использование
```powershell
# Для прокси-сервера (порт 8080)
lt --port 8080

# Вы получите URL типа: https://random-name.loca.lt
```

### Преимущества
- ✅ Очень простой в использовании
- ✅ Не требует регистрации
- ✅ Устанавливается через npm
- ✅ Бесплатный

### Недостатки
- ⚠️ URL меняется при каждом запуске
- ⚠️ Может быть нестабильным при долгой работе
- ⚠️ **Требует пароль (IP адрес) для доступа** - не подходит для Telegram Mini Apps

---

## 🥈 Cloudflare Tunnel (cloudflared) - самый надежный

### Установка на Windows

**Вариант 1: Через Chocolatey (требует админ права)**
```powershell
choco install cloudflared
```

**Вариант 2: Прямая загрузка (без админ прав)**
1. Скачайте: https://github.com/cloudflare/cloudflared/releases/latest
2. Распакуйте `cloudflared-windows-amd64.exe` в папку (например, `C:\Users\user\cloudflared\`)
3. Добавьте в PATH или используйте полный путь

### Использование
```powershell
# Для прокси-сервера (порт 8080)
cloudflared tunnel --url http://localhost:8080

# Вы получите URL типа: https://random-name.trycloudflare.com
```

### Преимущества
- ✅ Очень надежный (от Cloudflare)
- ✅ Быстрый (глобальная сеть Cloudflare)
- ✅ Бесплатный
- ✅ Стабильный

### Недостатки
- ⚠️ URL меняется при каждом запуске (на бесплатном плане)
- ⚠️ Требует установки клиента

---

## 🥉 localhost.run (через SSH, без установки)

### Использование
```powershell
# Если у вас установлен SSH клиент (обычно есть в Windows 10+)
ssh -R 80:localhost:8080 ssh.localhost.run

# Или через PowerShell (если установлен OpenSSH)
ssh -R 80:localhost:8080 ssh.localhost.run
```

### Преимущества
- ✅ Не требует установки (если SSH уже есть)
- ✅ Простой в использовании
- ✅ Бесплатный

### Недостатки
- ⚠️ Требует SSH клиент
- ⚠️ Может быть нестабильным

---

## 🚀 Pinggy.io (веб-интерфейс)

### Использование
1. Откройте https://pinggy.io
2. Скопируйте команду для вашей ОС
3. Запустите в терминале

Или через PowerShell:
```powershell
# Скачайте клиент с https://pinggy.io/download
# Или используйте веб-версию
```

### Преимущества
- ✅ Веб-интерфейс для управления
- ✅ Логи запросов
- ✅ Бесплатный план

---

## 📋 Быстрый старт с Localtunnel (рекомендуется)

### 1. Установите localtunnel
```powershell
npm install -g localtunnel
```

### 2. Запустите прокси-сервер
```powershell
cd C:\Users\user\Desktop\Loyverse
node proxy-server-express.js
```

### 3. В другом терминале запустите туннель
```powershell
lt --port 8080
```

### 4. Скопируйте полученный URL
Например: `https://random-name.loca.lt`

### 5. Обновите .env в backend
```env
CLIENT_APP_URL=https://random-name.loca.lt/app
OWNER_APP_URL=https://random-name.loca.lt/owner
CASHIER_APP_URL=https://random-name.loca.lt/cashier
```

### 6. Настройте Mini Apps
```powershell
cd backend
npm run setup-mini-apps
```

---

## 🔧 Настройка для вашего проекта

После выбора альтернативы, обновите:

1. **backend/.env** - URL приложений
2. **shared/config.js** - API URL (или через `VITE_API_URL`)
3. Запустите `npm run setup-mini-apps` в backend

---

## 💡 Сравнение

| Инструмент | Простота | Надежность | Стабильность | Рекомендация |
|------------|----------|------------|--------------|--------------|
| **Localtunnel** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | Для быстрого тестирования |
| **Cloudflare Tunnel** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | Для долгой работы |
| **localhost.run** | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | Если SSH уже установлен |
| **Pinggy.io** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | Если нужен веб-интерфейс |

---

## 🎯 Моя рекомендация

**Для Telegram Mini Apps (требуется доступ без пароля):**
1. **Cloudflare Tunnel** - не требует пароля, самый надежный ✅
2. **localhost.run** - если SSH уже установлен
3. **Pinggy.io** - если нужен веб-интерфейс

**⚠️ Localtunnel НЕ рекомендуется** для Telegram Mini Apps, так как требует пароль для доступа.

---

## 🚀 Быстрый старт с Cloudflare Tunnel

### 1. Установите Cloudflare Tunnel

**Windows (без админ прав):**
1. Скачайте: https://github.com/cloudflare/cloudflared/releases/latest
2. Распакуйте `cloudflared-windows-amd64.exe`
3. Переименуйте в `cloudflared.exe`
4. Поместите в `C:\Users\user\cloudflared\`

### 2. Запустите прокси-сервер
```powershell
cd C:\Users\user\Desktop\Loyverse
node proxy-server-express.js
```

### 3. Запустите Cloudflare Tunnel
```powershell
C:\Users\user\cloudflared\cloudflared.exe tunnel --url http://localhost:8080
```

### 4. Скопируйте полученный URL
Например: `https://random-name.trycloudflare.com`

### 5. Обновите `backend/.env`
```env
CLIENT_APP_URL=https://random-name.trycloudflare.com/app
OWNER_APP_URL=https://random-name.trycloudflare.com/owner
CASHIER_APP_URL=https://random-name.trycloudflare.com/cashier
```

### 6. Настройте Mini Apps
```powershell
cd backend
npm run setup-mini-apps
```
