# 🔧 Исправление цикла редиректов (ERR_TOO_MANY_REDIRECTS)

## Проблема
Ошибка "ERR_TOO_MANY_REDIRECTS" при открытии приложения через Cloudflare Tunnel.

## Причина
Конфликт между:
- `base: '/app/'` в `vite.config.js` (для production)
- Прокси, который передает запросы на Vite dev server
- React Router с `basename="/app"`

В dev режиме Vite с `base: '/app/'` может создавать цикл редиректов.

## Решение

### 1. Обновлен `app/vite.config.js`

Теперь `base` зависит от режима:
- **Dev режим:** `base: '/'` (корневой путь)
- **Production:** `base: '/app/'` (для деплоя)

### 2. Обновлен `proxy-server-express.js`

Прокси теперь убирает `/app` из пути при проксировании на Vite:
- Запрос: `/app/` → Прокси отправляет на `http://localhost:3000/`
- Vite обрабатывает на корневом пути
- React Router все еще использует `basename="/app"` для правильной маршрутизации

### 3. Перезапустите Vite dev server для app

**Важно:** Нужно перезапустить Vite dev server, чтобы изменения вступили в силу!

```powershell
# Остановите текущий Vite (Ctrl+C)
# Затем запустите заново:
cd app
npm run dev
```

### 4. Проверьте работу

После перезапуска Vite:
```powershell
# Локально
curl http://localhost:8082/app/

# Через туннель
curl https://turns-bloomberg-greatest-caused.trycloudflare.com/app/
```

Оба должны вернуть HTML страницу (не редирект).

---

## ⚠️ Важно

- **Обязательно перезапустите Vite dev server** после изменения `vite.config.js`
- React Router все еще использует `basename="/app"` - это правильно
- В production `base: '/app/'` останется для правильного деплоя

## 🔍 Если проблема сохраняется

1. Проверьте, что Vite перезапущен
2. Проверьте логи прокси - не должно быть множества запросов к `/app/`
3. Проверьте, что app работает напрямую на `http://localhost:3000/`
